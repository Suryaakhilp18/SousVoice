/**
 * Shared Motion Tokens for Consistent Animation Language
 */
export const motionTokens = {
  fast: 0.18,
  base: 0.28,
  screen: 0.35,

  micro: { duration: 0.2, ease: 'easeOut' as const },
  card: { duration: 0.28, ease: 'easeOut' as const },
  screenTransition: { duration: 0.35, ease: 'easeOut' as const },
};
